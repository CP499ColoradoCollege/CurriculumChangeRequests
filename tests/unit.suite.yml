# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - Asserts
        - \Helper\Unit
        # - Db: 
        #     dsn: 'mysql:host=localhost;dbname=testDB'
        #     user: 'root'
        #     password: ''
        #     dump: 'tests/_data/testdump.sql'
        #     populate: true
        #     cleanup: true
        #     # initial_queries:
        #     #  - 'CREATE DATABASE IF NOT EXISTS testDB;'
        #     populator: '/opt/lampp/bin/mysql -u $user -h $host $dbname < $dump'